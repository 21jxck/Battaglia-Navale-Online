// enum che indica lo stato del match
enum StatoPartita {
  posizionamento,
  inGioco,
  finita
}

// enum che indica lo stato di un colpo
enum EsitoColpo {
  mancato,    // nave non presa
  colpito,    // nave colpita
  affondato   // nave affondata
}
